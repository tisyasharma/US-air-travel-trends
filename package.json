{
  "name": "us-air-travel-trends",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git worktree add .gh-pages gh-pages && rm -rf .gh-pages/* && cp -R dist/* .gh-pages/ && cd .gh-pages && git add -A && git commit -m \"Deploy\" && git push origin gh-pages && cd .. && git worktree remove .gh-pages"
  },
  "dependencies": {
    "d3": "^7.9.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5"
  }
}
